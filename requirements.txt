# Hole9Wiki backend dependencies
# 安装: pip install -r requirements.txt
# 版本基准为当前测试通过版本,>= 允许向后兼容的小升级。

# ---- Web 框架 (server/server.py) ----
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.7

# ---- 环境变量 (server/server.py, agent.py, neo4j_mcp_server.py, build_wiki_index.py) ----
python-dotenv>=1.0

# ---- MCP 子服务器 (ask_user / neo4j / wiki-reader) ----
# fastmcp 框架通过 `from mcp.server.fastmcp import FastMCP` 引入
mcp>=1.0

# ---- Neo4j 图谱查询 (markdown_parser.py, server/neo4j_mcp_server.py) ----
# 未配置 Neo4j 时运行时会跳过,但模块本身 import 阶段就需要
neo4j>=5.0

# ---- Wiki 路由索引 LLM 生成 (build_wiki_index.py) ----
# 可选: 无 key 时可用 `--no-llm` 纯规则模式,但模块 import 该包
openai>=1.50

# ---- 注意 ----
# Claude Code CLI 是独立工具,不通过 pip 安装。
# 参考: https://docs.anthropic.com/en/docs/claude-code
#
# 前端依赖见 frontend/package.json,通过 `npm install` 安装。
